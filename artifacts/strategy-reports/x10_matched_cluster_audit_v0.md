# X-10 Matched-Cluster Audit v0

- **Owner:** Team G1 + Team A; veto owner Team H
- **Version:** v0
- **Due gate:** `2026-08-05_W2_review` for core first artifacts
- **Status:** `REGISTERED_NOT_RUN`

The workflow imports PMXT Router pairs, validates Team A's closed relation taxonomy, creates a deterministic low-confidence-first review queue, and computes precision only after every sampled pair is adjudicated exactly once.

The registered 50-pair sample, Router/taxonomy version, gold-standard protocol, and H approval locks are unresolved. No 50-pair dataset or precision result is fabricated here. The formal runner also binds the exact sample and code hashes to the X-10 preregistration.

The numeric gate is 45/50 correct. Passing permits further G1 research only. It never authorizes simultaneous multi-venue execution or live arbitrage. Recall is refused until a candidate-universe denominator is separately registered.
